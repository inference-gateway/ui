# SQLite Storage Configuration
STORAGE_TYPE=sqlite
DB_CONNECTION_URL=sqlite:/data/chat_history.db

# Application Configuration
INFERENCE_GATEWAY_URL=http://inference-gateway:8080
LOG_LEVEL=info

# Optional: Authentication (set to true to enable)
AUTH_ENABLE=false

# Optional: NextAuth configuration (if AUTH_ENABLE=true)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-here